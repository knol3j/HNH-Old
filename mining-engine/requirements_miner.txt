# Requirements for Hashnhedge_miner.exe

# Core dependencies
requests>=2.31.0
pyinstaller>=6.0.0

# GUI (included with Python on Windows)
# tkinter - bundled with Python

# Optional for building
# pillow>=10.0.0  # If adding custom icons
