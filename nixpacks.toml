[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npx prisma generate']

[start]
cmd = 'npx prisma migrate deploy || echo "Migration failed or already applied, continuing..." && npm start'
