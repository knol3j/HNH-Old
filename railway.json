{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd hybrid-pool && npm install"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd hybrid-pool && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
